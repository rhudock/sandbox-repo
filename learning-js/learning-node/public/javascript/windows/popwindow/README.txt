
Testing the followings

1. open a new window (pop up).
2. communication between two windows.
3. if child window knows when parent window's url changes.
    3.1 send message to the child window when the window is closeing.



browser names \ function can use on exit.

accessing a's href on onbeforeunload event
evt.target.activeElement.href






